# sahou:stub node=sensor
# sahou:hash debug_tap=2277930d615c5ac8
# sahou:hash get_state=661d3f2d7fa87cfc
# sahou:hash points=cf97f73a020f14ed
# sahou:hash touch=fab35f00a7338b1c
"""AUTO-GENERATED by `sahou gen --lang python --node sensor`. Do not edit by hand (it will be regenerated).

Static layer only: the engine does not read this file. The runtime behaves identically without the stub.
drift detection is `sahou check` (CLI/CI responsibility; design §8/§13).

Usage (app code): pip install sahou, then use its connect():
    node = typed_node(sahou.connect("gen/descriptor.json", node="sensor"))
    node.publish(...) / node.subscribe(...) / node.query_confirmed(...) / node.answer(...)
"""
from __future__ import annotations

from typing import Any, Literal, NotRequired, TypedDict

SCHEMA_HASHES: dict[str, str] = {
    "debug_tap": "2277930d615c5ac8",
    "get_state": "661d3f2d7fa87cfc",
    "points": "cf97f73a020f14ed",
    "touch": "fab35f00a7338b1c",
}


class GetStateRequest(TypedDict):
    sel: str

class GetStateResponse(TypedDict):
    level: int

class Points(TypedDict):
    pts: list[list[float]]

class Touch(TypedDict):
    x: float
    phase: Literal["down", "move", "up"]
    meta: TouchMeta

class TouchMeta(TypedDict):
    ts: int
    source: NotRequired[str]


def typed_node(node):
    """Identity (zero runtime cost). The .pyi declares it returns SensorNode, so IDE completion works."""
    return node

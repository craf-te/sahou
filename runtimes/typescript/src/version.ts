/** The sahou npm package version, reported in vitals. Kept in lockstep with package.json
 *  "version" by tests/version.test.ts (a committed constant because the browser entry has
 *  no filesystem to read package metadata from at runtime). */
export const SAHOU_VERSION = "0.0.2";

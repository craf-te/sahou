[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax).
# Intel macOS (x86_64-apple-darwin) dropped — Apple Silicon only (matches the Python wheel matrix).
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Where to host releases
hosting = "github"
# Keep our hand-edited release.yml trigger (narrowed to `v*` so runtime tags don't
# cross-fire). Without this, `dist generate` would reset the `on: push: tags` pattern.
allow-dirty = ["ci"]
